You are using the program `%s` to execute the following task: %s.

Context and parameters for execution:

```json
%s
```

Generate and execute a shell command to complete this task.
The command should be precise and focused on the specific task requirements.
